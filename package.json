{
  "name": "blueworx-client-toptiertutors",
  "version": "0.1.0",
  "private": true,
  "description": "Top Tier Tutors WordPress plugin.",
  "scripts": {
    "lint": "node --check playwright.config.js && node --check tests/global-setup.js && node --check tests/smoke.spec.js",
    "build": "echo \"No build step yet — assets ship as authored\" && exit 0",
    "test": "playwright test",
    "wp:up": "node ../bluegroup_core_foundation/scripts/wp-test-env.mjs up --plugin . --slug blueworx-client-toptiertutors --dir .wp-test --port 8881",
    "wp:down": "node ../bluegroup_core_foundation/scripts/wp-test-env.mjs down --dir .wp-test"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.1"
  }
}
